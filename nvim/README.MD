# READ FIRST
1. Install `pynvim` through either pip or the package manager (python 3 is required)
2. Install `nodejs`
3. Install `python-pylint` or `pip install pylint`
3. Make sure to run `:PlugInstall`
4. Make sure to run `:CocInstall coc-marketplace coc-clangd coc-discord-rpc coc-python`
